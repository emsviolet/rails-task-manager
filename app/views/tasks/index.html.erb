<h1>My tasks</h1>
<% @tasks.each do |task| %>
  <ul style="list-style: none">
    <li>
      <%= check_box_tag(:title) %>
      <%= link_to each_task_path(task.id) do %>
        <%= label_tag(:title, "#{task.title}") %>
      <% end  %>
    </li>
  </ul>
<% end %>
